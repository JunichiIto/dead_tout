require "spec_helper"

RSpec.describe DeadTout do
  it "has a version number" do
    expect(DeadTout::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
